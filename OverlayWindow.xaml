<Window x:Class="WpfVideoPet.OverlayWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        AllowsTransparency="True"
        Background="Transparent"
        WindowStyle="None"
        ShowActivated="False"
        ShowInTaskbar="False"
        Topmost="True"
        ResizeMode="NoResize"
        Focusable="False">

    <!-- Grid 主容器，禁止鼠标交互（悬浮展示层） -->
    <Grid IsHitTestVisible="False">

        <!-- 🟦 左上角天气信息面板 -->
        <Border HorizontalAlignment="Left"
                VerticalAlignment="Top"
                Margin="16"
                Padding="10"
                CornerRadius="8"
                Background="#66000000">
            <StackPanel>
                <TextBlock x:Name="TxtCity" Text="城市: -" Foreground="White" FontSize="16" FontWeight="Bold"/>
                <TextBlock x:Name="TxtWeather" Text="天气: -" Foreground="White" FontSize="14"/>
                <TextBlock x:Name="TxtTemp" Text="气温: -" Foreground="White" FontSize="14"/>
                <TextBlock x:Name="TxtTime" Text="时间: -" Foreground="White" FontSize="12" Opacity="0.9"/>
            </StackPanel>
        </Border>

        <!-- 🟥 右下角 3D 模型区域（Core DX11 渲染输出） -->
        <Border HorizontalAlignment="Right"
                VerticalAlignment="Bottom"
                Margin="0,0,20,20"
                Padding="0"
                Background="#00000000"
                ClipToBounds="False">
            <Grid Width="600"
                  Height="720"
                  VerticalAlignment="Bottom"
                  ClipToBounds="False">
                <Image x:Name="PetImage"
                       Stretch="Uniform"
                       HorizontalAlignment="Right"
                       VerticalAlignment="Bottom"
                       Margin="0"
                       RenderTransformOrigin="0.5,0.95"
                       Focusable="False"
                       IsHitTestVisible="False"
                       RenderOptions.BitmapScalingMode="HighQuality"
                       SnapsToDevicePixels="True">
                    <Image.RenderTransform>
                        <TransformGroup>
                            <ScaleTransform x:Name="PetImageScaleTransform" ScaleX="1" ScaleY="1"/>
                            <TranslateTransform x:Name="PetImageTranslateTransform" X="0" Y="0"/>
                        </TransformGroup>
                    </Image.RenderTransform>
                </Image>
            </Grid>
        </Border>
    </Grid>
</Window>